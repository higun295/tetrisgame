<Window
    x:Name="window"
    x:Class="Tetris.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Tetris"
    xmlns:converter="clr-namespace:Tetris.Converter"
    mc:Ignorable="d"
    Title="MainWindow"
    Height="700"
    Width="500"
    WindowStartupLocation="CenterScreen"
    >

    <Window.Resources>
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverer" />
    </Window.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="KeyDown">
            <i:InvokeCommandAction Command="{Binding KeyDownCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid
        Margin="10"
        Visibility="{Binding IsGameStart, Converter={StaticResource BoolToVisibilityConverer}}"
        Background="Gray"
        >
        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="Press any key to start game"
            FontSize="20"
            />
    </Grid>
</Window>